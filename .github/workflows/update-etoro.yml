name: Aggiorna prezzi eToro su Firestore

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'   # ogni ora

jobs:
  update-portfolio:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run update-etoro script
        env:
          # eToro Public API
          ETORO_PUBLIC_API_KEY: ${{ secrets.ETORO_PUBLIC_API_KEY }}
          ETORO_USER_KEY: ${{ secrets.ETORO_USER_KEY }}

          # Firebase (usa UNA sola chiave)
          FIREBASE_KEY_BASE64: ${{ secrets.FIREBASE_KEY_BASE64 }}
        run: |
          node scripts/update-etoro.js
